name: Netlify Build

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    name: Request Build
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/5fff8f89314dfb8e32ae72de